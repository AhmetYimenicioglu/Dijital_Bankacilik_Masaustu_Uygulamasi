<UserControl x:Class="CyberBank.Views.CardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1100">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="28,20,28,32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Card Visual & Debt Summary -->
            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <TextBlock Text="Kartlarım" FontSize="22" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,4"/>
                <TextBlock Text="Kredi ve Banka Kartı Yönetimi" FontSize="13" Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,20"/>

                <!-- Interactive Card Visual -->
                <Border CornerRadius="18" Height="220" Padding="24" Margin="0,0,0,16">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1E293B" Offset="0"/>
                            <GradientStop Color="#0F172A" Offset="0.5"/>
                            <GradientStop Color="#0284C7" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Top row -->
                        <Grid Grid.Row="0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="34" Height="26" Background="#E2E8F0" CornerRadius="4" Margin="0,0,8,0">
                                    <Border Width="22" Height="16" BorderBrush="#94A3B8" BorderThickness="1" CornerRadius="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="📶" Foreground="#CBD5E1" FontSize="18" VerticalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Border x:Name="BadgeCardStatus" Background="#10B981" CornerRadius="10" Padding="8,2" Margin="0,0,10,0">
                                    <TextBlock x:Name="TxtCardStatus" Text="AKTİF" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                </Border>
                                <TextBlock Text="CYBER PLATINUM" FontWeight="Bold" FontSize="13" Foreground="#38BDF8" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <!-- Card Number -->
                        <StackPanel Grid.Row="1" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtCardNumberDisplay" Text="5412 **** **** 9942" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                            <Button Content="👁 Numarayı Göster/Gizle" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left" Padding="8,3" FontSize="11" Margin="0,8,0,0" Click="BtnToggleMask_Click"/>
                        </StackPanel>

                        <!-- Bottom row: Holder, Expiry, CVV -->
                        <Grid Grid.Row="2">
                            <StackPanel>
                                <TextBlock Text="KART SAHİBİ" FontSize="9.5" Foreground="#94A3B8"/>
                                <TextBlock x:Name="TxtCardHolder" Text="DEMO KULLANICI" FontSize="13" FontWeight="SemiBold" Foreground="White"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <StackPanel Margin="0,0,20,0">
                                    <TextBlock Text="SON KULLANMA" FontSize="9.5" Foreground="#94A3B8" HorizontalAlignment="Right"/>
                                    <TextBlock x:Name="TxtExpiry" Text="08/29" FontSize="13" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Right"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="CVV" FontSize="9.5" Foreground="#94A3B8" HorizontalAlignment="Right"/>
                                    <TextBlock x:Name="TxtCvv" Text="***" FontSize="13" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Debt & Limits Card -->
                <Border Style="{StaticResource CardPanel}">
                    <StackPanel>
                        <TextBlock Text="Hesap Özeti &amp; Limitler" FontSize="15" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,14"/>

                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Toplam Kart Limiti" Foreground="{StaticResource BrushTextSecondary}" FontSize="13"/>
                            <TextBlock x:Name="TxtTotalLimit" Text="₺ 75,000.00" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" HorizontalAlignment="Right"/>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Kullanılabilir Limit" Foreground="{StaticResource BrushTextSecondary}" FontSize="13"/>
                            <TextBlock x:Name="TxtAvailLimit" Text="₺ 56,820.00" FontWeight="Bold" Foreground="{StaticResource BrushEmerald}" FontSize="13" HorizontalAlignment="Right"/>
                        </Grid>

                        <Grid Margin="0,0,0,14">
                            <TextBlock Text="Güncel Dönem Borcu" Foreground="{StaticResource BrushTextSecondary}" FontSize="13"/>
                            <TextBlock x:Name="TxtDebt" Text="₺ 18,180.00" FontWeight="Bold" Foreground="{StaticResource BrushDanger}" FontSize="14" HorizontalAlignment="Right"/>
                        </Grid>

                        <Button Content="💳 Dönem Borcunu Öde" Style="{StaticResource PrimaryButton}" Height="40" FontSize="13" Click="BtnPayDebt_Click"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Right: Card Security Settings -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="Kart Güvenlik &amp; Kullanım Ayarları" FontSize="20" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,4"/>
                <TextBlock Text="Kartınızı anında kilitleyebilir veya alışveriş izinlerini yönetebilirsiniz" FontSize="13" Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,20"/>

                <!-- Setting 1: Lock Card -->
                <Border Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="42" Height="42" CornerRadius="10" Background="#3B1219" Margin="0,0,14,0" VerticalAlignment="Center">
                            <TextBlock Text="🔒" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Kartı Geçici Kilitle" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}"/>
                            <TextBlock Text="Kartınızı kayıp/şüpheli durumlarda tüm işlemlere anında kapatın" FontSize="11.5" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <CheckBox x:Name="ChkLockCard" Grid.Column="2" VerticalAlignment="Center" Cursor="Hand" Click="ChkLockCard_Click"/>
                    </Grid>
                </Border>

                <!-- Setting 2: Online Shopping -->
                <Border Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="42" Height="42" CornerRadius="10" Background="#162A45" Margin="0,0,14,0" VerticalAlignment="Center">
                            <TextBlock Text="🌐" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="İnternet Alışverişi (E-Ticaret)" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}"/>
                            <TextBlock Text="Yurt içi ve yurt dışı internet sitelerinden yapılan harcamalar" FontSize="11.5" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <CheckBox x:Name="ChkOnlineShopping" Grid.Column="2" IsChecked="True" VerticalAlignment="Center" Cursor="Hand" Click="ChkSettings_Click"/>
                    </Grid>
                </Border>

                <!-- Setting 3: Contactless -->
                <Border Style="{StaticResource CardPanel}" Margin="0,0,0,12" Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="42" Height="42" CornerRadius="10" Background="#13362E" Margin="0,0,14,0" VerticalAlignment="Center">
                            <TextBlock Text="📶" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Temassız Ödeme (NFC)" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}"/>
                            <TextBlock Text="POS cihazlarında şifresiz temassız ödeme özelliği" FontSize="11.5" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <CheckBox x:Name="ChkContactless" Grid.Column="2" IsChecked="True" VerticalAlignment="Center" Cursor="Hand" Click="ChkSettings_Click"/>
                    </Grid>
                </Border>

                <!-- Setting 4: Limit Adjustment -->
                <Border Style="{StaticResource CardPanel}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Kart Limiti Güncelleme" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,6"/>
                        <TextBlock Text="Toplam kart limitinizi dilediğiniz zaman artırıp azaltabilirsiniz" FontSize="11.5" Foreground="{StaticResource BrushTextMuted}" Margin="0,0,0,12"/>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TxtLimitInput" Style="{StaticResource ModernTextBox}" Height="40" FontSize="14" FontWeight="SemiBold" Text="75000" Margin="0,0,10,0"/>
                            <Button Grid.Column="1" Content="Limiti Güncelle" Style="{StaticResource SecondaryButton}" Height="40" Padding="14,0" Click="BtnUpdateLimit_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
