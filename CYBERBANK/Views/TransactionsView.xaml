<UserControl x:Class="CyberBank.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1100">
    <Grid Margin="28,20,28,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header & Income/Expense Stats -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Hesap Hareketleri" FontSize="22" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,4"/>
                <TextBlock Text="Tüm hesaplarınıza ait detaylı gelir ve gider hareketleri" FontSize="13" Foreground="{StaticResource BrushTextMuted}"/>
            </StackPanel>

            <!-- Stats -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Style="{StaticResource CardPanel}" Padding="14,10" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Toplam Gelir: " Foreground="{StaticResource BrushTextSecondary}" FontSize="12" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtTotalIncome" Text="+ ₺63,700.00" Foreground="{StaticResource BrushEmerald}" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardPanel}" Padding="14,10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Toplam Gider: " Foreground="{StaticResource BrushTextSecondary}" FontSize="12" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtTotalExpense" Text="- ₺21,350.29" Foreground="{StaticResource BrushDanger}" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Search & Category Filters Bar -->
        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="14" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Search Input -->
                <TextBox x:Name="TxtSearch" Style="{StaticResource ModernTextBox}" Height="38" Text="" TextChanged="TxtSearch_TextChanged" Margin="0,0,16,0" Tag="İşlem veya alıcı ara..."/>

                <!-- Category Filters -->
                <WrapPanel Grid.Column="1" VerticalAlignment="Center">
                    <Button Content="Tümü" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" Click="CategoryFilter_Click" Tag="All"/>
                    <Button Content="Transferler" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" Click="CategoryFilter_Click" Tag="Transfer"/>
                    <Button Content="Maaş / Gelir" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" Click="CategoryFilter_Click" Tag="Maaş"/>
                    <Button Content="Market" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" Click="CategoryFilter_Click" Tag="Market"/>
                    <Button Content="Kira" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" Click="CategoryFilter_Click" Tag="Kira"/>
                    <Button Content="Fatura" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" Click="CategoryFilter_Click" Tag="Fatura"/>
                    <Button Content="Döviz" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" Click="CategoryFilter_Click" Tag="Döviz"/>
                </WrapPanel>
            </Grid>
        </Border>

        <!-- Transactions List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="TransactionsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardPanel}" Margin="0,0,0,8" Padding="14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon -->
                                <Border Width="42" Height="42" CornerRadius="10" Background="#16253D" Margin="0,0,14,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding IconText}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <!-- Title, Desc, Category -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,10,0"/>
                                        <Border Background="#1E293B" CornerRadius="6" Padding="6,2" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Category}" FontSize="10.5" Foreground="{StaticResource BrushCyan}"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{StaticResource BrushTextMuted}" Margin="0,3,0,0"/>
                                    <TextBlock Text="{Binding FormattedDate}" FontSize="11" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0"/>
                                </StackPanel>

                                <!-- Receipt Ref -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,24,0" HorizontalAlignment="Right">
                                    <TextBlock Text="Dekont No" FontSize="10" Foreground="{StaticResource BrushTextMuted}" HorizontalAlignment="Right"/>
                                    <TextBlock Text="{Binding ReceiptNumber}" FontSize="11.5" Foreground="{StaticResource BrushTextSecondary}" HorizontalAlignment="Right"/>
                                </StackPanel>

                                <!-- Amount -->
                                <TextBlock Grid.Column="3" Text="{Binding FormattedAmount}" Foreground="{Binding AmountColor}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
