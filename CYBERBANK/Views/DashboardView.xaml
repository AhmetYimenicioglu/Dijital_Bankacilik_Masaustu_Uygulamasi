<UserControl x:Class="CyberBank.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1100">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="28,20,28,32">
            <!-- Top Banner: Total Assets -->
            <Border CornerRadius="16" Padding="24" Margin="0,0,0,24">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#0F2942" Offset="0"/>
                        <GradientStop Color="#163859" Offset="0.5"/>
                        <GradientStop Color="#0D9488" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="TOPLAM FİNANSAL VARLIKLARIM" FontSize="12" FontWeight="SemiBold" Foreground="#A5F3FC" Margin="0,0,0,4"/>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtTotalWealth" Text="₺ 354,450.75" FontSize="32" FontWeight="Bold" Foreground="White" VerticalAlignment="Bottom"/>
                            <Border Background="#10B981" CornerRadius="12" Padding="8,3" Margin="14,0,0,4" VerticalAlignment="Center">
                                <TextBlock Text="Aktif Portföy" FontSize="11" FontWeight="Bold" Foreground="White"/>
                            </Border>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <TextBlock Text="Vadesiz TL: " Foreground="#94A3B8" FontSize="12"/>
                            <TextBlock x:Name="TxtTlSummary" Text="₺84,450.75" Foreground="White" FontSize="12" FontWeight="SemiBold" Margin="0,0,16,0"/>

                            <TextBlock Text="Döviz &amp; Yatırım: " Foreground="#94A3B8" FontSize="12"/>
                            <TextBlock x:Name="TxtForeignSummary" Text="$3,450.00 / €1,680.00" Foreground="White" FontSize="12" FontWeight="SemiBold" Margin="0,0,16,0"/>

                            <TextBlock Text="Vadeli Mevduat: " Foreground="#94A3B8" FontSize="12"/>
                            <TextBlock x:Name="TxtTermSummary" Text="₺150,000.00" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Fast Action ATM simulation button -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <Button Content="🏧 ATM / Para Yatır-Çek" Style="{StaticResource SecondaryButton}" Padding="16,10" Click="BtnAtm_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Middle Section: Accounts & Credit Card (2 columns) -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.3*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Accounts Section -->
                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <TextBlock Text="Hesaplarım" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,12"/>
                    <ItemsControl x:Name="AccountsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource CardPanel}" Margin="0,0,0,10" Padding="16">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="44" Height="44" CornerRadius="10" Background="#162A45" Margin="0,0,14,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Currency}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource BrushEmerald}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}"/>
                                            <TextBlock Text="{Binding Iban}" FontSize="12" Foreground="{StaticResource BrushTextMuted}" Margin="0,3,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding FormattedBalance}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" HorizontalAlignment="Right"/>
                                            <TextBlock Text="{Binding AccountType}" FontSize="11" Foreground="{StaticResource BrushEmerald}" HorizontalAlignment="Right" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!-- Credit Card Preview -->
                <StackPanel Grid.Column="1" Margin="16,0,0,0">
                    <TextBlock Text="Kredi Kartım" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,12"/>
                    
                    <!-- Visual Card -->
                    <Border CornerRadius="16" Height="190" Padding="20" Margin="0,0,0,14">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#1E293B" Offset="0"/>
                                <GradientStop Color="#0F172A" Offset="0.6"/>
                                <GradientStop Color="#0284C7" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Top: Brand & Chip -->
                            <Grid Grid.Row="0">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Width="30" Height="22" Background="#E2E8F0" CornerRadius="4" Margin="0,0,8,0">
                                        <Border Width="18" Height="14" BorderBrush="#94A3B8" BorderThickness="1" CornerRadius="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="📶" Foreground="#CBD5E1" FontSize="16" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="CYBER PLATINUM" FontWeight="Bold" FontSize="12.5" Foreground="#38BDF8" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Middle: Card Number -->
                            <TextBlock Grid.Row="1" x:Name="TxtCardNumber" Text="5412 **** **** 9942" FontSize="18" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>

                            <!-- Bottom: Holder & Expiry -->
                            <Grid Grid.Row="2">
                                <StackPanel>
                                    <TextBlock Text="KART SAHİBİ" FontSize="9" Foreground="#94A3B8"/>
                                    <TextBlock x:Name="TxtCardHolder" Text="DEMO KULLANICI" FontSize="12" FontWeight="SemiBold" Foreground="White"/>
                                </StackPanel>
                                <StackPanel HorizontalAlignment="Right">
                                    <TextBlock Text="SON KULLANMA" FontSize="9" Foreground="#94A3B8" HorizontalAlignment="Right"/>
                                    <TextBlock x:Name="TxtCardExpiry" Text="08/29" FontSize="12" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Limit and Debt summary -->
                    <Border Style="{StaticResource CardPanel}" Padding="16">
                        <StackPanel>
                            <Grid Margin="0,0,0,6">
                                <TextBlock Text="Kullanılabilir Limit" FontSize="12" Foreground="{StaticResource BrushTextSecondary}"/>
                                <TextBlock x:Name="TxtAvailableLimit" Text="₺ 56,820.00" FontSize="13" FontWeight="Bold" Foreground="{StaticResource BrushEmerald}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,10">
                                <TextBlock Text="Güncel Dönem Borcu" FontSize="12" Foreground="{StaticResource BrushTextSecondary}"/>
                                <TextBlock x:Name="TxtCardDebt" Text="₺ 18,180.00" FontSize="13" FontWeight="Bold" Foreground="{StaticResource BrushDanger}" HorizontalAlignment="Right"/>
                            </Grid>
                            <ProgressBar x:Name="PrgCardLimit" Height="7" Value="75" Maximum="100" Background="#1E293B" Foreground="{StaticResource BrushCyan}" BorderThickness="0" Margin="0,0,0,14"/>
                            <Button Content="💳 Kart Borcunu Öde" Style="{StaticResource PrimaryButton}" Height="38" FontSize="13" Click="BtnPayDebt_Click"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Quick Action Tiles -->
            <TextBlock Text="Hızlı İşlemler" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,12"/>
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Margin="0,0,8,0" Style="{StaticResource SecondaryButton}" Height="80" Click="BtnGoTransfer_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="💸" FontSize="24" HorizontalAlignment="Center"/>
                        <TextBlock Text="Para Transferi (FAST)" FontSize="13" FontWeight="SemiBold" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Grid.Column="1" Margin="4,0,4,0" Style="{StaticResource SecondaryButton}" Height="80" Click="BtnGoCards_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="💳" FontSize="24" HorizontalAlignment="Center"/>
                        <TextBlock Text="Kart İşlemleri" FontSize="13" FontWeight="SemiBold" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Grid.Column="2" Margin="4,0,4,0" Style="{StaticResource SecondaryButton}" Height="80" Click="BtnGoExchange_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="💱" FontSize="24" HorizontalAlignment="Center"/>
                        <TextBlock Text="Döviz &amp; Altın Al/Sat" FontSize="13" FontWeight="SemiBold" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Grid.Column="3" Margin="8,0,0,0" Style="{StaticResource SecondaryButton}" Height="80" Click="BtnGoBills_Click">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🧾" FontSize="24" HorizontalAlignment="Center"/>
                        <TextBlock Text="Fatura Ödeme" FontSize="13" FontWeight="SemiBold" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- Recent Transactions Preview -->
            <Grid Margin="0,0,0,12">
                <TextBlock Text="Son Hesap Hareketleri" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" VerticalAlignment="Center"/>
                <Button Content="Tümünü Gör →" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" Padding="12,6" FontSize="12" Click="BtnAllTransactions_Click"/>
            </Grid>

            <ItemsControl x:Name="RecentTransactionsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardPanel}" Margin="0,0,0,8" Padding="14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="40" Height="40" CornerRadius="10" Background="#16253D" Margin="0,0,12,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding IconText}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontSize="13.5" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}"/>
                                    <TextBlock Text="{Binding Description}" FontSize="11.5" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding FormattedDate}" FontSize="10.5" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0"/>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="{Binding FormattedAmount}" Foreground="{Binding AmountColor}" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
